version: "3.9"
services:
  web:
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - "5000:5000"
    depends_on:
      - redis
    image: web
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
